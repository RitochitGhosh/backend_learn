const math = require('./math.js'); // dont use `require('math')` it will look for math in the built-in packages

console.log(math.mul(2, 4));
console.log(math);


console.log(__dirname);
console.log(__filename);


